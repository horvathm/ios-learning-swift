///
/// Arrays
///

// Declaration
var fruits = ["strawberries", "limes", "tangerines"]

// Indexing and assignmemnt
fruits[1] = "grapes"

// Reading
print(fruits[0])

// Empty array
fruits = []

/*
 - Collections are mutable by default. This means that you can change (or mutate) the collection after it’s created by adding, removing, or changing items in the collection
 - good practice to create immutable collections in all cases where the collection doesn’t need to change. If you assign an array, a set, or a dictionary to a constant...
 */
